---
import { ROLES, checkRole } from '../functions/checkRole';

const user = Astro.locals.user;

if (!checkRole(user, [ROLES.ESTUDIANTE])) {
  return Astro.redirect('/403');
}

// Redirect to their own patient profile
return Astro.redirect(`/patients/${user.userId}`);
---
